// This file handle items that may need to be accessible globally. Don't get confused, dont have to do with adding funcionalty directly or uniquely to the main HTML elements.

const pageSections = document.querySelectorAll("header, main, footer");

export {pageSections};